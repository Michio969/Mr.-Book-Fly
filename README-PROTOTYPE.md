Prototype branch: Storybook scaffold and sample GlassCard component

What was added:
- .storybook (main.js, preview.js, manager.js)
- src/components/GlassCard.tsx (sample component)
- src/components/stories/GlassCard.stories.tsx (Storybook story)
- package.json: storybook scripts + devDependencies (no packages installed yet)

How to run locally (after npm install):
1. npm install
2. npm run storybook

Notes:
- No runtime installs were run; devDependencies were only added to package.json. Run `npm install` locally to test Storybook.
- This branch is a prototype workspace; code changes will be made only after your approval.
